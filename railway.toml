[build]
builder = "dockerfile"
dockerfilePath = "backend/FiltCar.Api/Dockerfile"

[deploy]
startCommand = "dotnet FiltCar.Api.dll"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
